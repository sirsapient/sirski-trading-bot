# Wallet Configuration
# NOTE: These are placeholder private keys - you need the actual private keys for these addresses
# SOLANA_PRIVATE_KEY=[41, 49, 63, 12, 43, 15, 43, 254, 192, 220, 201, 47, 49, 254, 151, 238, 165, 54, 172, 252, 24, 108, 240, 240, 218, 133, 76, 36, 99, 107, 179, 8]
# BASE_PRIVATE_KEY=0x56e5d86c44539355f6825160cb438ba2007dad040bf1398d162ddecf7df0cfc1

# Funded Wallet Addresses (for reference)
SOLANA_FUNDED_ADDRESS=9JnHrkYXY8KV49vb4ps2SRZ2mBZ8AsK4XSHn3wi3ANfc
BASE_FUNDED_ADDRESS=0x2D21399C95753AbEFCeF2100239CaF3fa6661492

# RPC Endpoints (use free tiers to start)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
BASE_RPC_URL=https://mainnet.base.org

# Trading Parameters (Optimized for Paper Trading)
MIN_ARBITRAGE_PROFIT=0.001    # 0.1%
MAX_POSITION_SIZE=0.2         # 20% of portfolio (more conservative)
MIN_TRADE_SIZE=100           # $100 minimum for paper trading
MAX_DAILY_LOSS=0.03          # 3% daily loss limit (more conservative)

# Monitoring
DISCORD_WEBHOOK_URL=your_discord_webhook_for_alerts

# Jupiter API (for Solana price data)
JUPITER_API_URL=https://price.jup.ag/v4

# Uniswap V3 API (for Base price data)
UNISWAP_V3_API_URL=https://api.thegraph.com/subgraphs/name/uniswap/uniswap-v3

# Trading pairs to monitor
SOLANA_PAIRS=SOL/USDC,ETH/USDC,RAY/USDC
BASE_PAIRS=ETH/USDC,WETH/USDC

# Risk management (Conservative for Paper Trading)
STOP_LOSS_PERCENTAGE=0.02    # 2% stop loss (more conservative)
TAKE_PROFIT_PERCENTAGE=0.03  # 3% take profit (more conservative)
MAX_SLIPPAGE=0.003          # 0.3% max slippage (more conservative)

# RPC Optimization Settings (Slower for Paper Trading)
ARBITRAGE_SCAN_INTERVAL=60      # 1 minute (slower for paper)
SWING_SCAN_INTERVAL=600         # 10 minutes (slower for paper)
PRICE_CACHE_DURATION=300        # 5 minutes (longer cache)
BALANCE_CHECK_INTERVAL=7200     # 2 hours (less frequent)

# Paper trading specific
TRADING_MODE=paper              # "paper" or "live"
PAPER_ARBITRAGE_INTERVAL=60     # 1 minute
PAPER_SWING_INTERVAL=600        # 10 minutes
PAPER_CACHE_DURATION=300        # 5 minutes
